name: 'Auto Release Draft'
description: 'Drafts a github release with the changes introduced by a tag'
author: 'Prasanna Bhat'
outputs:
  release-url:
    description : 'The URL of the GitHub release that was drafted'
runs:
  using: 'node12'
  main: 'dist/index.js'
